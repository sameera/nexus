"""The Nexus Python toolkit — the capabilities reached by the name `nexus-gh` (epic #249).

Held out of the collapse onto the TypeScript executable because every import here is standard
library: there is no package install to fail and no resolver to supply, so these ship as files
and stay a second toolkit rather than becoming verbs on the first.
"""

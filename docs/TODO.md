- [x] Remove Business Value section from Epic. This is speculative generation from the LLM - nobody specifies it.
- [ ] Change Prime into a React Router 7 SSR as now there anyway needs to be a server running for PTY bridge.
- [ ] ~~Add a settings file to Claude so that all plans get written to that and have distiller also read them as decision records.~~ Rejected 2026-07-03 — see `.nexus/queue/9c91bf1f/decision.md`. Deferred weaker form: opt-in per-engineer hook → gitignored `.nexus/plans/` scratch dir, consulted by `/nxs.close` as hints only. Trigger: a close-from-diff pass failing to reconstruct implementation rationale.
- [ ] Formalize decision-only queue entries as a `/nxs.decide` skill (capture out-of-band decisions into the queue; distiller drains diff-less entries into concept decision logs). Trigger: hand-written memos become frequent.
- [x] ~~Should the Distiller Queue use a path such as {user}/{branch}?~~ Resolved 2026-07-03 — flattened to `.nexus/queue/<local-id>/`, no branch or user tier. Random 8-hex ids merge cleanly across branches/authors without collision; a branch tier only baked a transient branch name into a committed path (fragmenting trunk after merge) and username added nothing git blame doesn't already give. Branch/author scoping, if ever needed, belongs in epic frontmatter, not the path.
- [ ] Why are we writing the queue items to .nexus/ folder when they are meant to be read by a human?

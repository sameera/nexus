---
concept: published-verdict-selection
source_sha: 4586ae1c13dd1c6efee6d2044b59969cf968edbb
generated: 2026-09-21
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Published Verdict Selection

- `libs/pr-acceptance/src/receipt-blocks.ts` — the shared collection and ordering step both readers call: every marker-bearing review or comment with the platform timestamp of the body carrying it, trust filtering applied before recency, newest last (#753)
- `libs/pr-acceptance/src/receipt-blocks.spec.ts` — coverage for the split timestamp keys, the filter-then-rank order, and the newest-wins pick (#753)
- `libs/pr-acceptance/src/verify.ts` — the single-pull-request reader, now also filtering on maintainer authorship and the pull request a block names, and comparing the stamped repository through the one shared comparison (#753)
- `libs/epic-verdicts/src/verdict.ts` — the per-story reader, which applies the same trust and recency rule across every candidate pull request and no longer compares a stamped repository by string equality (#753)
- `libs/epic-verdicts/src/pr-verdict.ts` — the callable reader behind the command: one pull request in, the verdict it carries plus its currency out (#753)
- `libs/epic-verdicts/src/pr-verdict.spec.ts` — coverage for selection, the absent-verdict case, and the currency note (#753)
- `libs/pr-acceptance/src/verdict-fixtures.ts` — the live payload pinned as a fixture, published so both readers' specs run against the same one: no reviews, two maintainer comments oldest-first, the later omitting the optional release key (#753)
- `libs/workspace/src/issue-ref.ts` — the one repository comparison, beside the issue-reference grammar: owner and name must agree, and a host is compared only when both sides state one (#753)
- `libs/portable-tools/src/nexus-cli.ts` — the verb that exposes the reader to a stage, and the argument guard that refuses when the caller does not name the repository being read (#753)
- `libs/portable-tools/src/verdict-selection.spec.ts` — pins that the stages invoke the command and that neither restates the selection rule as prose (#753)
- `components/commands/nxs.close.md` — the close gate, which invokes the command and reports what it returns rather than opening the pull request's reviews and comments (#753)
- `components/skills/nxs-distill-recovery/SKILL.md` — the recovery path, which reads a linked pull request's verdict through the same command (#753)

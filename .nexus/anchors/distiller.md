---
concept: distiller
source_sha: 697e6042fd11dda4dcddc5d81b0398d422e79ac3
generated: 2026-08-27
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Distiller

- `.claude/commands/nxs.distill.md` — the drain agent: preflight and the run-mode gate, discovery across the committed queue and the ephemeral area, per-mode diff recomputation (single-repo range-first, introducing-commit scan as fallback), the single-entry continuation mode on a close-prepared distill branch, the two-test merge precondition for an entry absent from the trunk, explicit per-entry GitHub recovery from the epic issue's close comment, the removal re-aimed at the committed scratch dir for an ephemeral entry, per-concept delta synthesis (best-fit domain filing and the three-way taxonomy gate), the deterministic steps (reciprocity, anchor refresh, atlas, validator, drift advisory, run through one in-repo invocation in every mode), the ban on reading per-user scratch, checkpoint, and PR; resolves the why per entry — the record issue body hash-verified against the close stamp (hard-error on mismatch, no waiver), else a committed decision record, else the close record — and follows the resolved docs root for the atlas.
- `libs/portable-tools/src/derive-entry-diff.ts` — hub-mode diff derivation, now asked for as a verb on the installed toolkit: reads an entry's recorded range, resolves each repo to its member checkout, and emits one diff per repo with the queue and the discovery store both excluded (read-only; a missing checkout or unreachable revision is a hard error, never a fallback or partial diff).
- `libs/portable-tools/src/derive-entry-diff.spec.ts` — pins both exclusions, including that discovery prose riding the same branch as the code never reaches the emitted diff.
- `libs/portable-tools/src/validate-concepts.ts` — the deterministic validator (concept pages and anchor sidecars) a drain must pass before the distillation-PR opens.
- `libs/portable-tools/src/generate-atlas.ts` — the atlas regeneration step the drain runs before each entry's commit; writes to the resolved docs root the drain then stages and reports.
- `libs/portable-tools/src/drift-advisory.ts` — the non-blocking advisory the drain runs once over the final store state at its checkpoint, pasting findings into the PR body.
- `.claude/commands/nxs.distill.md` — the reciprocity fan-out, page-patching, split, and validator steps as rewritten for the own-content cap: no drop, demote, or compress to fit; two edges when one bullet will not hold the interaction; split on own-content overflow alone; a non-zero exit blocks and an advisory does not.

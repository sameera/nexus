---
concept: distiller
source_sha: b8d5deba61114fe14e2cbaab98a4df798e612f48
generated: 2026-07-31
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Distiller

- `.claude/commands/nxs.distill.md` — the drain agent: preflight and the run-mode gate, discovery across the committed queue and the ephemeral area, per-mode diff recomputation (single-repo range-first, introducing-commit scan as fallback), the single-entry continuation mode on a close-prepared distill branch, the two-test merge precondition for an entry absent from the trunk, explicit per-entry GitHub recovery from the epic issue's close comment, the removal re-aimed at the committed scratch dir for an ephemeral entry, per-concept delta synthesis (best-fit domain filing and the three-way taxonomy gate), the deterministic steps (reciprocity, anchor refresh, atlas, validator, drift advisory, hub-vs-single-repo invocation), the ban on reading per-user scratch, checkpoint, and PR; resolves the why per entry — the record issue body hash-verified against the close stamp (hard-error on mismatch, no waiver), else a committed decision record, else the close record — and follows the resolved docs root for the atlas.
- `libs/portable-tools/src/derive-entry-diff.ts` — hub-mode diff derivation: reads an entry's recorded range, resolves each repo to its member checkout, and emits one queue-excluded diff per repo (read-only; a missing checkout or unreachable revision is a hard error, never a fallback or partial diff).
- `libs/portable-tools/src/validate-concepts.ts` — the deterministic validator (concept pages and anchor sidecars) a drain must pass before the distillation-PR opens.
- `libs/portable-tools/src/generate-atlas.ts` — the atlas regeneration step the drain runs before each entry's commit; writes to the resolved docs root the drain then stages and reports.
- `libs/portable-tools/src/drift-advisory.ts` — the non-blocking advisory the drain runs once over the final store state at its checkpoint, pasting findings into the PR body.

---
concept: distiller
source_sha: b74a97736ebc340d427f50c946a53594a2124c90
generated: 2026-09-20
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Distiller

- `components/commands/nxs.distill.md` — the drain agent: preflight and the run-mode gate, discovery across the committed queue and the ephemeral area, diff recomputation through the one range reader as the sole source in either mode, the single-entry continuation mode on a close-prepared distill branch, the two-test merge precondition for an entry absent from the trunk, explicit per-entry GitHub recovery from the epic issue's close comment, the removal re-aimed at the committed scratch dir for an ephemeral entry, per-concept delta synthesis (best-fit domain filing and the three-way taxonomy gate), the deterministic steps (reciprocity, anchor refresh, atlas, validator, drift advisory), the ban on reading per-user scratch, checkpoint, and PR; resolves the why per entry — the record issue body hash-verified against the close stamp (hard-error on mismatch, no waiver), else a committed decision record, else the close record.
- `components/commands/nxs.distill.md` — Phase 1 after the fallback removal: a non-zero exit from the derivation step blocks that one entry whatever the failure class, with no second diff source in either mode, no operator-supplied replacement range, and no partial or empty diff.
- `components/commands/nxs.distill.md` — the entry-kind contract: one table stating the four axes on which epic, fix and intake differ, the kind resolved once at discovery and recorded in the entry's run scratch, and every later phase reading that recorded value rather than re-deriving it.
- `components/commands/nxs.distill.md` — the run summary written to the run scratch, defining each value once with its own omission rule and zero case; the checkpoint, the pull-request body and the completion report are layouts naming those fields.
- `components/commands/nxs.distill.md` — the drain-health report: an undrained entry attributed to every distinct repository its range list names in first-appearance order, one age figure per entry measured from the introducing commit, and a blocked entry naming the range entry that could not be resolved.
- `components/commands/nxs.distill.md` — the prose convention with its grounding variant: the rule block sits directly above the drafting step, writes the fetched record body to a named file at preflight, and drafts each changed page and the PR body plain the first time, grounding an abstraction in the epic and that why file rather than translating a finished draft.
- `components/commands/nxs.distill.md` — the Input Resolution scan exempts a planning draft under `.nexus/tmp/planning/` by construction: it carries no `close-record.md` and its own `epic.md`, when one exists, has no `entry_kind:` this scan recognises.
- `components/commands/nxs.distill.md` — run-shape resolution and the contract selection table: the run mode, the workspace shape and each entry's recorded kind resolved before anything is reported, refused or written, and each exceptional path's rules read from its own contract only once its condition resolves (#734).
- `libs/portable-tools/src/distill-rule-once.spec.ts` — pins each consolidated rule's single statement and the absence of its second copy: the unreachable-revision contract, the entry-kind contract, the run summary behind the three surfaces, and the removed closing recap.
- `libs/portable-tools/src/derive-entry-diff.ts` — the reader the drain derives every entry's change sets through, in both modes.
- `libs/portable-tools/src/derive-entry-diff.spec.ts` — pins the exclusions, including that discovery prose riding the same branch as the code never reaches the emitted diff.
- `libs/portable-tools/src/validate-concepts.ts` — the deterministic validator a drain must pass before the distillation-PR opens.
- `libs/portable-tools/src/generate-atlas.ts` — the atlas regeneration step the drain runs before each entry's commit.
- `libs/portable-tools/src/drift-advisory.ts` — the non-blocking advisory the drain runs once over the final store state at its checkpoint.
- `libs/portable-tools/src/pipeline-stores.ts` — the exclusion set the drain's behavioural diff reads, in place of paths written into the command body.
- `components/commands/nxs.intake.md` — the third entry-writing command the drain discovers and drains: its two-file entry shape, its `pr_digest` stamp, and its absence of a decision record, a receipt or a process lesson.
- `components/skills/nxs-landed-reference/SKILL.md` — the shared resolution rules an intake entry's provenance already satisfies before the drain ever reads it.
- `libs/portable-tools/src/nxs-intake.spec.ts` — pins the intake command's derivations, refusals, gate, and the entry shape it writes, with its kind rules re-aimed at the one entry-kind contract.
- `libs/portable-tools/src/fix-lane.spec.ts` — pins the fix command's entry shape, with its kind rules re-aimed at the same one contract so each kind's coverage stays attributable.

---
concept: on-demand-stage-contract
source_sha: b74a97736ebc340d427f50c946a53594a2124c90
generated: 2026-09-20
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: On-Demand Stage Contract

- `components/commands/nxs.distill.md` — the base stage: run-shape resolution at the front of input resolution, the contract selection table mapping each resolved condition to the one contract it selects, and the statement that the ordinary shape selects none (#734).
- `components/skills/nxs-distill-recovery/SKILL.md` — the recovery-mode contract, selected from the resolved run mode (#734).
- `components/skills/nxs-distill-continuation/SKILL.md` — the close hand-off contract, selected from the resolved run mode after the entry list (#734).
- `components/skills/nxs-distill-hub/SKILL.md` — the hub-workspace contract, selected from the resolved workspace shape (#734).
- `components/skills/nxs-distill-nonepic-entries/SKILL.md` — the non-epic entry-kind contract, selected when a discovered entry's recorded kind is fix or intake; carries the two non-epic rows of the entry-kind comparison (#734).
- `components/skills/nxs-distill-taxonomy/SKILL.md` — the domain-registry contract, selected by the store survey's registry-presence check (#734).
- `libs/portable-tools/src/distill-load-ceiling.ts` — the size check and the residency check, and the list of rules each contract must own (#734).
- `libs/portable-tools/distill-load-ceiling.json` — the recorded ceiling: the measured bytes, the date recorded, and the value it replaces (#734).
- `libs/portable-tools/src/distill-load-ceiling.spec.ts` — asserts the measurement covers the base stage plus every contract's description and no contract body, that it stays at or under the ceiling, and that each check names what failed (#734).
- `libs/portable-tools/src/distill-exceptional-paths.spec.ts` — asserts the selection table is complete, that each contract is named only in the table and at its own gate, that each describes itself by its stage and selecting condition, and that the ordinary shape selects nothing (#734).

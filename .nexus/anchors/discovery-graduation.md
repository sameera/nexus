---
concept: discovery-graduation
source_sha: 45f665a559cafcbb27daf99725ba4b1049fb116b
generated: 2026-08-11
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Discovery Graduation

- `.claude/commands/nxs.epic.md` — the `--discovery` entry mode: the finished-discovery preconditions, the discovery document as intent, the skipped sharpness test and unchanged right-size gate, the feature default read from the discovery document, the gist copied into each stub body, the marked gist comment posted after filing, the same comment on the epic issue for the directly-planned path, and the consumed-folder report that deletes nothing.
- `.claude/commands/nxs.decision-record.md` — the read side: fetching the epic issue's comments, keeping only the marked ones, and passing them to the architect as already-settled decisions.
- `.claude/skills/nxs-gh-create-story/scripts/create_gh_issues.py` — the one batch filing path a graduated discovery's stubs go through unchanged.
- `.claude/skills/nxs-gh-create-epic/scripts/nxs_gh_create_epic.py` — the promotion path, unchanged by graduation: it rewrites the stub's body and leaves the marked comment alone.

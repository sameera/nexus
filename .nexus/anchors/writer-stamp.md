---
concept: writer-stamp
source_sha: bc452aa34f9fac29a4af6e74e5fe97b044d08974
generated: 2026-08-26
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Writer Stamp

- `libs/portable-tools/src/writer-stamp.ts` — the contract in one module: the single field name, the unknown-writer value, the emitted stamp line (none when the release is unresolved), and the reader that maps an absent or blank field to unknown.
- `libs/portable-tools/src/writer-stamp.spec.ts` — the surface enumeration: every writing and reading surface pinned against the one field name, plus the absent-stamp and differing-version readings.
- `libs/pr-acceptance/src/verify.ts` — the published-review receipt block's parser, which now carries the writer field and maps an absent one to null.
- `libs/pr-acceptance/src/verify.spec.ts` — pins that a stamped receipt block parses to the same values as an unstamped one, so the stamp invalidates no hash.
- `.claude/commands/nxs.analyze.md` — the receipt writer: the field in both the local receipt frontmatter and the published machine block, with the omit-when-unresolved rule.
- `.claude/commands/nxs.close.md` — the close record and close-comment machine block writers, including the rule that the command's field list outranks a tuned project template's placeholder set.
- `common/templates/close-record-template.md` — the deployable close-record template carrying the field beside the record hash.

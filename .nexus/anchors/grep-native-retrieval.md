---
concept: grep-native-retrieval
source_sha: 278fd9e248d582fcc2b76f7eda124234799716e5
generated: 2026-08-04
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Grep-Native Retrieval

- `libs/portable-tools/src/drift-advisory.ts` — reopens community detection but confines it to offline, advisory-only proposals of missing domains, never persisted as page metadata or read as an index, so retrieval stays grep-native.
- `libs/portable-tools/src/generate-atlas.ts` — builds the one derived orientation surface (the atlas) that no tool retrieves through; retrieval remains search, list, and read over the readable pages.
- `libs/portable-tools/src/validate-concepts.ts` — bounds the neighbour list per entry instead of against the page cap, so a blast-radius edge is never dropped for space; the high-degree advisory names a hub without failing it.
- `libs/origin/v2/.nexus/decisions/0003-concept-schema.md` — §2.2's statement that a real interaction is always declarable as an edge, and §4's neighbour-list and retrieval-cap contract.

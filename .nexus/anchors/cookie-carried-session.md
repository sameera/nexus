---
concept: cookie-carried-session
source_sha: adcccd89905720e930fb2c9e44e11970447e7760
generated: 2026-09-19
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Cookie-Carried Session

- `apps/renderer/src/session.ts` — the sealing and opening of the value, the two cookie names carrying the browser-enforced prefix, and the cookie attributes each one is set with (#661)
- `apps/renderer/src/session.spec.ts` — coverage for a value sealed and reopened, a value sealed under another key, and the attributes every cookie is set with (#661)
- `apps/renderer/src/config.ts` — the start-time reading of the sealing key, which refuses a missing or wrong-sized one rather than falling back (#661)
- `apps/renderer/src/config.spec.ts` — coverage for what an instance needs before it can sign a reader in (#661)
- `apps/renderer/src/exchange.ts` — where the credential's life is taken from the host's own answer, with the stated fallback the session's life is then set to (#661)
- `apps/renderer/src/exchange.spec.ts` — coverage for the returned credential and its life, including the fallback (#661)
- `apps/renderer/src/lambda.ts` — the translation both ways, where a request's separately delivered cookies are put back into the one header read and the response's own cookie header is removed in favour of the field the host returns (#667)
- `apps/renderer/src/lambda.spec.ts` — coverage for a session read from the separately delivered cookies and for what a response sets on the browser arriving in that field alone (#667)
- `apps/renderer/src/environment.ts` — the one assembly of what an instance takes from its environment, which both the developer's listener and the deployed function are handed (#667)
- `apps/renderer/src/environment.spec.ts` — coverage for that assembly refusing to start rather than sealing sessions under a key of its own (#667)

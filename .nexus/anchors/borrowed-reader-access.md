---
concept: borrowed-reader-access
source_sha: 6b8cc36db60b655ef5fabfcd4b619202f10ce330
generated: 2026-09-18
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Borrowed Reader Access

- `apps/renderer/src/handler.ts` — the one request path: the served-store check before any fetch, the fetch made with or without the reader's credential, the turned-away branch that starts a sign-in only once, and the cache marking that follows from a usable session (#661)
- `apps/renderer/src/config.ts` — the configured store list and size cap an instance is handed at start, which bound the stores served and answer nothing about any reader (#661)
- `apps/renderer/src/reader.spec.ts` — coverage for a private mockup served under the reader's own access, and for each reader's access being answered again (#661)
- `apps/renderer/src/handler.spec.ts` — coverage for what is served, what is refused, and what is cached, including the credentialed response that must not be stored (#661)
- `apps/renderer/src/listen.spec.ts` — coverage for the listener a developer runs, assembled from the environment alone (#661)

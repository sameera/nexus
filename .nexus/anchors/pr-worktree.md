---
concept: pr-worktree
source_sha: 56c14a215e4927aa0d955fa4010962e1481b82ed
generated: 2026-09-08
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: PR Flow Worktree

- `libs/pr-worktree/src/worktree.ts` — the built-in temp base, the declared-value normalization, the resolver-seam lookup, the private prepare step both open paths funnel through, the in-repo/ignore and creatability gate, and idempotent create plus force-remove.
- `libs/pr-worktree/src/range-read.ts` — the range without the worktree: the same role gate, pull-request lookup and merge-safe derivation, run in the checkout itself, plus the shared best-effort head fetch the close path now also calls.
- `libs/pr-worktree/src/range-read.spec.ts` — exercises the merge, squash and rebase cases through the new entry point, including the changed-file-set criterion that separates a squash from a rebase.
- `libs/pr-worktree/src/worktree-base.spec.ts` — pins the byte-identical no-config base, quote/tilde/relative normalization, the per-checkout isolation segment, and each of the three refusals.
- `libs/pr-worktree/src/worktree.spec.ts` — proves idempotent worktree create and force-remove.
- `libs/pr-worktree/src/diagnostic.ts` — the typed problem union the base refusals joined.
- `libs/portable-tools/src/nexus-cli.ts` — declares the range read as a subverb beside the worktree-opening ones and rewires the close path onto the shared head fetch.
- `components/skills/nxs-pr-worktree/SKILL.md` — documents the base key, the no-config fallback, the named refusals, and the range read that creates nothing.
- `components/skills/nxs-landed-reference/SKILL.md` — a second caller of the range read, reached the same worktree-free way the fix lane already reads it.

---
concept: pr-worktree
source_sha: eb0da38c84e2b0b1689797ba3eb4b3163fd6fb10
generated: 2026-08-01
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: PR Flow Worktree

- `libs/pr-worktree/src/worktree.ts` — the whole concept: the built-in temp base, the declared-value normalization, the resolver-seam lookup, the private prepare step both open paths funnel through, the in-repo/ignore and creatability gate, and idempotent create plus force-remove from the main checkout.
- `libs/pr-worktree/src/worktree-base.spec.ts` — pins the byte-identical no-config base, the empty-output-means-unset convention across the resolver seam, quote/tilde/relative normalization, the per-checkout isolation segment, and each of the three refusals.
- `libs/pr-worktree/src/worktree.spec.ts` — proves idempotent worktree create and force-remove.
- `libs/pr-worktree/src/git-fixtures.ts` — fixture repos that carry the checkout's real resolver script, so the cross-language seam is asserted against the actual contract rather than a stub.
- `libs/pr-worktree/src/diagnostic.ts` — the typed problem union the three base refusals joined.
- `.claude/skills/nxs-gh-shared/delivery_config.py` — the shared resolver this asks for the base; reports the declared value verbatim or nothing at all.
- `libs/workspace/src/manifest.ts` — the hub-defaults allowlist that must list the key for a hub to declare it at all.
- `.claude/skills/nxs-pr-worktree/SKILL.md` — documents the base key, absolute-vs-relative anchoring, the no-config fallback, the prefer-outside-the-repo warning, and the named refusals.
- `.claude/skills/nxs-pr-worktree/scripts/pr_worktree.ts` — the shim entry whose open subcommand surfaces a refused base as a named diagnostic.

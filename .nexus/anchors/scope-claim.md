---
concept: scope-claim
source_sha: 845c8dcc84dc07f40349a1c1e30ca37fb3ed6e62
generated: 2026-09-12
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Claim of Scope

- `libs/pr-worktree/src/story-candidates.ts` — the one implementation of both rules: the shared scope vocabulary, the qualifier comparison used by both text rungs, the same-repository condition on the platform's closing links, the explicit reference that replaces collection, and the near misses a refusal prints (#572)
- `libs/pr-worktree/src/story-candidates.spec.ts` — coverage for a foreign-qualified reference never looked up, a bare number ignored in a body, a mention that claims nothing, the project vocabulary, and the explicit reference settling a run (#572)
- `libs/portable-tools/src/nexus-cli.ts` — reads the pull request's own repository from the target checkout and passes it in, which is what gates the platform's closing-link rung (#572)
- `components/commands/nxs.analyze.md` — the stage's own documented statement of the grammar, kept in step with what the resolver implements (#572)

---
concept: workspace-resolution
source_sha: 142dc6b0f8bd631b8f71dc44dbe25285413a57bf
generated: 2026-07-18
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Workspace Resolution

- `libs/workspace/src/resolve.ts` — the single resolver entry point: role dispatch by which artifact is present, single-repo fallback on absence of both, per-member checkout-state annotation, the hub's vendored portable-tools location, and each repo's docs root as resolved context, plus the thin docs-root-only selector consumers read.
- `libs/workspace/src/manifest.ts` — reads/parses/validates the hub manifest into the canonical workspace description; emits the structural diagnostics, rejects a member reusing the hub's name or remote, and validates the optional hub docs-root override.
- `libs/workspace/src/pointer.ts` — reads a member's hub pointer, locates the hub as a named sibling, and emits the member-side diagnostics (missing pointer, missing hub checkout, hub-remote mismatch, undeclared member).
- `libs/workspace/src/status.ts` — renders the status read-out (resolved workspace, single-repo mode, or failure) as plain terminal text, including each repo's docs root.
- `libs/workspace/package.json` — the shared `@nexus/workspace` package: subpath exports, no barrel.
- `.claude/skills/nxs-workspace-status/scripts/workspace_status.ts` — the read-out entry point that invokes the resolver and prints its rendering.
- `.claude/skills/nxs-workspace-status/SKILL.md` — the workspace status read-out skill (the observable surface).
- `.nexus/config/templates/workspace-template.yml` — the hub manifest template leads copy into a real hub; documents the optional docs-root override.
- `.nexus/config/templates/hub-template.yml` — the member pointer template.
- `.claude/skills/nxs-abs-doc-path/scripts/get_abs_doc_path.ts` — the doc-link builder that reads the resolver's docs root, strips exactly that prefix when converting a repo-relative path to an absolute URL, and errors when its configured URL disagrees with the resolved root.
- `.claude/skills/nxs-abs-doc-path/SKILL.md` — the cross-ref doc-link skill (docs-root strip and URL-agreement check).
- `libs/workspace/src/resolve.spec.ts` — parity, single-repo fallback, checkout-state, and docs-root resolution (incl. the docs-root-only selector).
- `libs/workspace/src/manifest.spec.ts` — hub-side parse/validate and diagnostics, including the member-vs-hub collision rule and the docs-root override.
- `libs/workspace/src/pointer.spec.ts` — member-side resolution and diagnostics.
- `libs/workspace/src/status.spec.ts` — read-out rendering across the three cases, including the docs root.
- `libs/portable-tools/src/cross-ref-docs-root.spec.ts` — subprocess coverage of the doc-link builder's docs-root strip and URL-agreement behavior in both layouts.

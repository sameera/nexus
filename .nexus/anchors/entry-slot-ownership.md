---
concept: entry-slot-ownership
source_sha: ba24f5ee9f0745474e583de1b3596d2444b5c1ca
generated: 2026-09-12
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Entry Slot Ownership

- `components/skills/nxs-landed-reference/SKILL.md` — the shared section itself: the closed three-kind set with its directory prefixes and owning commands, the epic-classification refusal, the repository-scoped slot test, the linked-number widening and its degraded fallback, and the same-kind reconciliation's three eligibility conditions (#549)
- `components/commands/nxs.fix.md` — applies the shared section at both points under its own name, and states that it has no refusal of its own over its own kind (#549)
- `components/commands/nxs.intake.md` — applies the same two points, ahead of its own pull-request-only refusal, and states the one lane-local condition: an entry that already recorded filed deferred-scope issues (#549)
- `libs/portable-tools/src/fix-lane.spec.ts` — pins the shared section's content and the fix lane's two call sites (#549)
- `libs/portable-tools/src/nxs-intake.spec.ts` — pins the intake lane's two call sites and its deferred-scope rewrite refusal (#549)
